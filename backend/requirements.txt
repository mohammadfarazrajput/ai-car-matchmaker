# AI Car Matchmaker — backend dependencies
#
# EXACT PINS ONLY. No >=, no ~=, no ranges.
# Constitution Principle V: a silent minor bump mid-build is a failure class a
# two-day project cannot absorb.
#
# Every version below was verified against PyPI on 2026-08-08.
# See specs/001-ai-car-matchmaker/research.md R9 — two pins carried in the
# original docs (ag-ui-langgraph>=0.1.0, copilotkit>=1.61.0) do not exist.

# --- Web framework -----------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.3
pydantic-settings==2.13.0
python-multipart==0.0.21
httpx==0.28.1
python-dotenv==1.2.1

# --- Agent harness -----------------------------------------------------------
deepagents==0.7.5
langchain==1.3.14
langchain-core==1.5.3
langgraph==1.2.10
langgraph-checkpoint==4.2.0

# --- LLM providers -----------------------------------------------------------
# AI Studio, NOT Vertex AI — different products. research.md R4.
# Major version 4; the 2.x snippets in docs/ do not apply.
langchain-google-genai==4.3.2
langchain-groq==1.1.1

# --- AG-UI transport ---------------------------------------------------------
# 0.0.42 is the latest release. docs/ pinned >=0.1.0, which cannot resolve.
ag-ui-langgraph==0.0.42
# PyPI 'copilotkit' is 0.1.94. The 1.66.x line is the npm package.
copilotkit==0.1.94

# --- MCP Apps ----------------------------------------------------------------
# FastMCP lives here. Absent from docs/ entirely.
mcp==2.0.0

# --- Templating (email adapter) ----------------------------------------------
jinja2==3.1.6

# --- Notification channels ---------------------------------------------------
# Resend, not SendGrid — SendGrid's free tier was retired 2025-05-27.
resend==2.16.0
slack-sdk==3.36.0
twilio==9.10.1                      # STRETCH (T094)

# --- Observability ----------------------------------------------------- STRETCH
arize-phoenix-otel==0.16.1
openinference-instrumentation-langchain==0.1.70
opentelemetry-sdk==1.42.0
opentelemetry-exporter-otlp==1.42.0

# --- Testing -----------------------------------------------------------------
pytest==9.1.1
pytest-asyncio==1.4.0
anyio==4.14.2
